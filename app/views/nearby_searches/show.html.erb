<%= render 'form' %>

<div id="map-canvas"></div>

<h3>Landmarks near to <%= @poi.name %> (ordered by distance)</h3>

<ol class="nearby">
  <% @nearby_pois.each do |poi| %>
    <%= content_tag :li, poi.name %>
  <% end %>
</ol>

<%= content_for :head do %>
  <%= render 'map' %>
<% end %>
